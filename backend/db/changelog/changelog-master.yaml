# The Scenery — Liquibase master changelog.
# Changesets are included in order; each lives in changesets/ as formatted SQL
# with a --rollback block (see DESIGN.md §3.5). First real changesets land at
# roadmap items 1.3–1.4.
databaseChangeLog: []
