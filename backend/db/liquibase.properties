# Local CLI runs (the docker-compose liquibase service passes these via env).
# Password comes from LIQUIBASE_COMMAND_PASSWORD (see .env); never commit secrets.
changelog-file: changelog/changelog-master.yaml
url: jdbc:mysql://localhost:3306/scenery
username: scenery_migrator
driver: com.mysql.cj.jdbc.Driver
